/**
 * @file
 * Shanda Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.shandaTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
